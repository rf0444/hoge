mkdir gitgit
cd gitgit/

git init
(work at master-branch)
touch hoge
git add -A
git commit

(work at branch "br1")
git checkout -b br1
touch piyo
git add -A
git commit

vi piyo 
git add -A
git commit

(work at master-branch)
git checkout master 
vi hoge
git add -A
git commit

(back to branch "br1")
git checkout br1
vi hoge
(modify, conflicting master's)
git add -A
git commit

(rebase br1 to master)
git rebase -i master 
(intaractive mode. start editor. save and exit.)
(conflict occures)
git status
(file "hoge" conflicts)
vi hoge
(resolve the conflict)
git add -A
git commit
git rebase --continue 
(continue rebasing)
(rebase finished)

